version: 2
updates:
  # npm — Astro site dependencies
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 10
    groups:
      astro-major:
        patterns:
          - "astro"
          - "@astrojs/*"
        update-types:
          - "major"
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # GitHub Actions — keeps SHA-pinned actions current
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      # Bundle all action updates together; each SHA bump is low-risk since
      # the workflows already pin by commit hash with a version comment.
      github-actions:
        patterns:
          - "*"
